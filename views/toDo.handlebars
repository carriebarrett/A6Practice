<h1>Hello {{name}} Here is your To Do List!</h1>
<h2>You have {{toDoCount}} items on your to do list.</h2>
<form method="post">
  <label for="name">To-Do Item</label>
  <input type="text" name="name" id="toDoName">
  <label for="city">City</label>
  <input type="text" name="city" id="toDoCity">
  <label for="temp">Minimum Temperature(degrees Celcius)</label>
  <input type="number" name="temp" id="toDoTemp">
  <input type="submit" name="Add Item" value="Add Item">
</form>
{{#if toDo}}
<ul>
  {{#each toDo}}
  <li>
    <h3>{{this.name}}</h3>
    <ul>
      <li>{{this.city}}</li>
      <li>{{this.temp}}</li>
      <li>Warm enough? {{this.tempOk}}</li>
    </ul>
    <form method="post">
      <input type="hidden" name="id" value="{{this.id}}">
      <input type="submit" name="Done" value="Mark as Done">
    </form>
  </li>
  {{/each}}
</ul>
{{/if}}
<script>a6styling.js</script>